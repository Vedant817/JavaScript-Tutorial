//Adding Number to a string
let a="Vedant"
let b=7
console.log(a+b);//String will get concatenated.
//In case of objects keys inside them can be changed but not the variable in case of constant.
const a1={
    name:"Vedant",
    section:7,
    Coder:true,
}
a1['friend']="Vanshay"
console.log(a1);//String will get concatenated.